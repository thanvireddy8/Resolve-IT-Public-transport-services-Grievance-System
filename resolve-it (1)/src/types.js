// Type definitions for the application
// This file exports object shapes used throughout the app

export {};

// ComplaintStatus: 'Pending' | 'In Progress' | 'Resolved'
// Complaint: { id, userId, userName, category, subject, description, status, date, assignedDepartment, departmentMessage, departmentResolutionStatus }
// Notification: { id, userId, complaintId, message, date, isRead, targetRole }
// User: { id, name, email, role }
// Feedback: { id, userId, userName, message, date }
